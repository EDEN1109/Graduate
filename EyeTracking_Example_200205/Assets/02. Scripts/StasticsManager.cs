/*
 * 통계화면 관련 스크립트 입니다.
 * 
 */

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StasticsManager : MonoBehaviour
{
    private int[] loadKill = new int[4];
    private float[] loadTime = new float[4];


    // Start is called before the first frame update
    void Awake()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
